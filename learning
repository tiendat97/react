 Content:
1, ECMAScript 201x - ES6;
Mot vai tinh nang quan trong ES6:
+ sses.
+ Arrow function.
+ Constant
+ Scope (variables, functions).
+ map() function.
+ Destructuring assignment.
+ Template string. (tuy chon, nhung no rat huu ich cho 

viec dinh dang chuoi)
2, Babel; 
3, React;(ReactRouter)
+ Xay dung 2 phan server/client rieng biet de lam quen 

voi viec call API.
+ Build Todo App(ap dung map, reduce, filter…)
Luu y: 
+ State
+ Props
+ React component lifecycle
+ Nhan thuc duoc “shouldComponentUpdate()”.
- ReactRouter: 

4, Redux;(Flux)
+Viet ung dung Todolist
5, MongoDB, Mongoose (Node);
6, Feathers.
**Co the xem them:
+ RxJS
+ Server rendering
+ GraphQL & Relay
+ JSON Graph & Falcor
